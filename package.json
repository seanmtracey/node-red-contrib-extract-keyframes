{
  "name": "node-red-contrib-extract-keyframes",
  "version": "0.0.1",
  "description": "Extract keyframes from a video file and send them to Watson for analysis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sean M. Tracey",
  "license": "MIT",
  "node-red": {
    "nodes": {
      "stream": "extract-keyframes.js"
    }
  },
  "dependencies": {
    "debug": "^3.1.0",
    "ffmpeg-static": "^2.0.0",
    "ffprobe-static": "^2.0.0",
    "fluent-ffmpeg": "^2.1.2",
    "uuid": "^3.1.0"
  }
}
